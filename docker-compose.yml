version: '3.8'

services:
  addressbook:
    build: .
    container_name: addressbook-toml
    ports:
      - "5000:5000"
    volumes:
      # Persistent storage for TOML files
      - ./data:/data
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
